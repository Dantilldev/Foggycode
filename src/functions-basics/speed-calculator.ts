function travelTime(distance: number, speed: number): number {
  // TODO: return distance / speed
  return distance / speed;
}

console.log(travelTime(150, 50)); // 3
console.log(travelTime(90, 45)); // 2

// Steg för steg:

// Skriv en funktion travelTime med parametrarna distance (nummer) och speed (nummer).
// Returnera resultatet av distance delat med speed.
// Returnera bara talet (tiden i timmar).

// Kort: Dela sträckan med hastigheten och returnera svaret.
