// Explicit type annotation
let greeting: string = "Hello World";
console.log(greeting);

// Inferred type from initial value
let firstName = "Dina"; // Hover to see: TypeScript infers string
console.log(firstName);

// Tydliga vs automatiskt gissade typer:
// greeting har en tydlig typ: string.
// firstName får sin typ gissad utifrån värdet.
