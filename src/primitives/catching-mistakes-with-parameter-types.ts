function double(n: number) {
  // Your code here
  return n * 2;
}

// Correct call
console.log(double(2));

// Incorrect call (keep it so you can see the error)
// console.log(double("2"));

// Uppgift:
// 1. Skriv funktionen double(n: number) som returnerar n * 2.
// 2. Anropa den korrekt med ett tal.
// 3. Anropa den felaktigt med en sträng (behåll raden för att se felet).
// 4. Logga resultatet från den korrekta anropet.
